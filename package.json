{
  "name": "postc-project",
  "version": "1.0.0",
  "description": "Amit Laderech post C project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start_client\" \"npm run start_server\"",
    "start_client": "cd client && npm run dev",
    "start_server": "cd server && npm run dev",
    "clean": "concurrently \"npm run clean_client\" \"npm run clean_server\"",
    "clean_client": "cd client && rmdir /s /q node_modules && del package-lock.json",
    "clean_server": "cd server && rmdir /s /q node_modules && del package-lock.json",
    "build": "concurrently \"npm run build_client\" \"npm run build_server\"",
    "build_client": "cd client && npm install",
    "build_server": "cd server && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ErezPaperny/PostC-Project.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ErezPaperny/PostC-Project/issues"
  },
  "homepage": "https://github.com/ErezPaperny/PostC-Project#readme",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
